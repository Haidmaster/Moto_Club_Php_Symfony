{% extends 'base.html.twig' %}

{% block title %}Marques
{% endblock %}


{% block body %}

	{{form_start(form)}}

	{{form_row(form.libelle)}}<br>

	<button type="submit" class="btn btn-success">Enregistrer</button>

	{{form_end(form)}} <br>

	<a href="{{ path('marque_index')}}" class="btn btn-primary">Retour à la liste</a>

{% endblock %}
